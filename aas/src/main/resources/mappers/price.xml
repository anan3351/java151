<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="price">
    <insert id="priceInsert" parameterType="java.util.HashMap">
 		insert into tb_price(show_id, seat_level, price, add_p)
		values (#{show_id}, #{seat_level_0}, #{seat_price_0}, #{add_p})
 	</insert>
</mapper>
